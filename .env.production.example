NEXT_PUBLIC_API_URL=https://nyama-api-production.up.railway.app/api/v1

# Must be the SAME value as the nyama-api Railway env var JWT_SECRET.
# The admin dashboard signs tokens with this secret so that Railway accepts
# them directly (see src/lib/admin-jwt.ts and nyama-api/src/auth/strategies/jwt.strategy.ts).
JWT_SECRET=
