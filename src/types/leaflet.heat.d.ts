declare module "leaflet.heat" {
  // Augmenting the global leaflet namespace is done side-effectfully.
  // Importing the module registers L.heatLayer at runtime.
  const _default: unknown;
  export default _default;
}
